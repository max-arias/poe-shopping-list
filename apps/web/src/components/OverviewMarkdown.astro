---
import { renderOverviewMarkdown } from "./overview-markdown";

interface Props {
  value: string;
}

const { value } = Astro.props;
---

<div class="list-overview prose prose-sm" set:html={renderOverviewMarkdown(value)}></div>
