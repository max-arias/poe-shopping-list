---
interface Props {
  href: string;
  label: string;
}

const { href, label } = Astro.props;
---

<a
  class="absolute top-0 left-[var(--board-gutter)] z-10 -translate-y-[140%] border border-field-ink bg-field-surface px-2.5 py-2 focus:translate-y-0"
  href={href}>{label}</a>
