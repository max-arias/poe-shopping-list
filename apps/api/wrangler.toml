name = "poe-shopping-list-api"
main = "src/index.ts"
compatibility_date = "2025-01-01"
compatibility_flags = ["nodejs_compat"]

[[d1_databases]]
binding = "DB"
database_name = "poe-shopping-list"
# Run `wrangler d1 create poe-shopping-list` to get your database_id and replace the placeholder below
database_id = "a895a678-21ec-4a71-a9da-d2fed88a93c1"
migrations_dir = "migrations"

[vars]
WEBSITE_URL = "https://poe-shopping-list-website.max-c82.workers.dev"

# Production secrets — set via `wrangler secret put <NAME>`:
#   JWT_SECRET
#   ADMIN_JWT_SECRET
#   POE_CLIENT_ID
#   POE_CLIENT_SECRET
